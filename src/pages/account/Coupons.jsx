import React from "react";

const Coupons = () => {
  return (
    <div>
      <div>
        <button>{"即將開始(0)"}</button>
        <button>{"可使用(0)"}</button>
        <button>{"已失效(0)"}</button>
      </div>
    </div>
  );
};

export default Coupons;
